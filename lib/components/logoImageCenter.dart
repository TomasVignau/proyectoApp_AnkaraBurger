import 'package:flutter/material.dart';

class Logoimagecenter extends StatelessWidget {
  const Logoimagecenter({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset("assets/images/LogoAnkara.png", height: 500),
    );
  }
}
